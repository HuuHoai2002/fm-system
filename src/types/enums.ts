export enum PropertyType {
  TEXT = "TEXT",
  LONG_TEXT = "LONG_TEXT",
  CHECKBOX = "CHECKBOX",
  SELECT = "SELECT",
  RADIO = "RADIO",
  DATE = "DATE",
  TIME = "TIME",
  DATETIME = "DATETIME",
  RANGE = "RANGE",
}

export enum EFormStatus {
  DRAFT = "DRAFT",
  PUBLISHED = "PUBLISHED",
  ARCHIVED = "ARCHIVED",
}

export enum EFormResponseStatus {
  PROCESSING = "PROCESSING",
  COMPLETED = "COMPLETED",
  REJECTED = "REJECTED",
}

export enum FormResponseProcessStatus {
  PROCESSING = "PROCESSING",
  COMPLETED = "COMPLETED",
  REJECTED = "REJECTED",
}
